# AI 활용 기술 문서 — 「Dough to Journey」

> NAN 2026 Game × AI 해커톤 사전 과제
> 저장소 https://github.com/RaphaelAhn/dough-dungeon
> 플레이 https://raphaelahn.github.io/dough-dungeon/

---

## 1. 요약

플레이어는 **아무것도 올라가지 않은 흰 도우**다. 재료 몬스터를 쓰러뜨려 토핑으로 올리고, 8라운드에 구워져 피자가 된다. 어떤 피자가 되는지는 그때까지의 선택이 정한다.

AI는 두 층위에서 쓰였다.

| 층위 | 무엇을 했나 | 증거 |
|---|---|---|
| **개발 과정** | 코드 전량 작성, 밸런스 수치 산출, **설계 결함 발견** | 커밋 34개, `tools/sim.ts` |
| **게임 내부** | 보상 3택 구성, 재료 등장 편향, 피자 판정, 절차적 명명 | `src/core/reward.ts`, `pizza.ts`, `codex.ts` |

**무게중심은 개발 과정 쪽에 있다.** 게임 안에 LLM을 호출하는 부분은 없다. 그렇게 만든 이유는 §2.1에 적었다.

가장 내세우고 싶은 것은 §4다. **AI가 사람이 놓친 설계 결함을 여섯 번 잡아냈고, 그때마다 규칙이 바뀌었다.** 그 과정이 커밋 기록에 그대로 남아 있다.

이 문서는 8/4 기준으로 작성됐다. 이후 8/9까지 프롤로그·화덕 연출·몬스터 표정·스토리 스킵 등을 추가로 붙였는데, 코어 규칙(전투 공식·재료 편향·보상·피자 판정)은 그대로이므로 이 문서의 설계 내용은 바뀌지 않았다 — 무엇을 언제 추가했는지는 `02_개발_일정표.md`에 정리했다.

---

## 2. 게임 내부의 AI

### 2.1 LLM을 쓰지 않은 이유

두 가지 이유로 배제했다.

**규정.** 사전 과제는 *"별도 유료 라이선스 없이 심사자가 실행할 수 있어야 한다"* 고 요구한다. 브라우저에서 도는 게임이 LLM을 호출하려면 API 키가 클라이언트 번들에 들어가고, 공개 저장소에 그 키가 그대로 노출된다. 키를 빼면 심사자 환경에서 게임이 동작하지 않는다. 어느 쪽도 조건을 만족하지 못한다.

**판정 가능성.** 매 실행 결과가 달라지면 밸런스를 측정할 수 없다. §3.2의 시뮬레이션은 같은 시드에 같은 결과가 나오는 것을 전제로 한다.

그래서 게임 내부의 판단은 **결정론적 규칙**으로 구현하고, AI는 그 규칙을 설계하고 검증하는 쪽에 썼다. 외부 의존이 0이라 심사자는 링크만 누르면 된다.

### 2.2 재료 95종을 표로 적지 않는다

야채 60 · 고기 20 · 소스 15 = 95종. 여기에 체력·공격력을 각각 적어 두지 않았다.

**규격은 라운드가 정하고, 재료는 이름과 맛만 준다.**

```ts
// src/core/enemy.ts
const SPEC = {
  1: { kind: 'normal', count: 1, hp: [108], atk: 9, spd: [9] },
  5: { kind: 'midboss', count: 1, hp: [264], atk: 14, spd: [12] },
  10: { kind: 'boss', count: 1, hp: [542], atk: 20, spd: [16] },
}
```

재료마다 수치를 박아 두면 같은 재료가 몇 라운드에 나오느냐에 따라 난이도가 널뛴다. 규격을 분리하면 95종을 아무 순서로 뽑아도 난이도 곡선이 유지된다. **적은 매 판 새로 뽑히므로 같은 판이 두 번 나오지 않는다.**

**한 판에서 만난 재료는 그 판에 다시 나오지 않는다.** 60종에서 14마리를 뽑으면 그냥 두었을 때 77.6% 확률로 겹친다. 만난 재료를 기록해 제외하면 0%가 된다 (§4.6).

일반 라운드는 야채, 5라운드 중간보스는 고기, 10라운드 보스는 소스에서 뽑는다.

### 2.3 맛 5종이 정체성을 만든다

야채/고기/소스 3종만으로 나누면 라운드 대부분이 야채라 피자의 성격이 갈리지 않는다. 그래서 재료마다 **맛**을 붙였다.

| 맛 | 능력치 | 부딪치는 맛 |
|---|---|---|
| 담백 | 신선도 | — |
| 매콤 | 반죽 탄력 | 새콤 |
| 새콤 | 촉감 | 매콤 |
| 향긋 | 신축성 | 진한 |
| 진한 | 반죽 두께 | 향긋 |

이 축 하나가 세 가지를 동시에 굴린다.

1. **쓸 수 있는 기술** — 올린 토핑의 맛이 곧 기술이다. 매콤을 올리면 '화끈한 일격', 진한을 올리면 '진한 풍미'
2. **속성 상성** — 부딪치는 맛으로 때리면 1.5배
3. **피자의 정체성** — 주 맛 × 부 맛 = 25종 (§2.5)

**담백은 어느 맛과도 부딪치지 않는다.** 이 비대칭이 전략의 폭을 만든다 — §4.4에서 이게 최적 전략을 바꿨다.

### 2.35 시작 능력은 '도우 숙성'이 정한다

게임을 시작하면 냉장 숙성실에서 도우를 **한 번만** 숙성시킨다. 온도계와 발효 게이지가 함께 흔들리다 멈추고, 그 조건이 시작 능력이 된다.

| 조건 | 결과 |
|---|---|
| 0℃ / 발효 100% | 보상 등급 2회 최고 확정 |
| 2℃ / 95% | 신선도 +40 |
| 5℃ / 88% | 반죽 두께 +12 |
| 6℃ / 78% | 촉감 +8 |
| 8℃ / 62% | 신축성 +8 |
| 9℃ / 70% | 반죽 탄력 +10 |

결과를 정하는 건 난수지만 화면에서는 **숙성 조건이 결과를 만든 것**으로 읽힌다. 저온·고발효일수록 좋은 조건이다.

> 처음에는 주사위를 굴리는 화면이었다. 피자 테마에서 주사위가 겉돌아 계기 두 개로 바꿨고, 게임 어디에도 주사위가 남지 않게 되어 타이틀의 장식 주사위도 함께 정리했다.

### 2.4 보상 3택 — 서로 대체 불가능한 세 슬롯

라운드 보상은 세 장을 제시하고 하나만 고르게 한다.

| 슬롯 | 성격 | 고르면 포기하는 것 |
|---|---|---|
| 장인의 손길 | 한 능력치를 크게 | 고른 성장 |
| 완벽한 숙성 | 고르게 조금씩, 또는 회복 | 한 방의 크기 |
| 무리한 반죽 | 크게 얻고 확실히 잃음 | 안전 |

핵심은 **"제일 센 것"이라는 답이 없게 만드는 것**이다. 세 장이 같은 축에 있으면 비교가 가능해지고, 비교가 가능하면 고민이 사라진다.

카드는 두 단계로 만든다.

1. **등급 추첨** — 수련 40% / 숙련 35% / 장인 20% / 명장 5%. 촉감이 하위 등급 확률을 상위로 옮긴다. 등급은 **세 장 각각 따로 굴린다** — 한 벌을 묶어 굴리면 '수련 지점'이 생겨 편차가 커진다.
2. **상태 반응** — 신선도가 60% 아래로 떨어지면 '숙성' 슬롯에 회복 카드가 확률적으로 등장한다.

보상이 나오는 라운드도 판마다 다르다. `[1, 3, 9]` 또는 `[2, 4, 8]` 중 하나를 추첨한다. 매 판 같은 자리에서 나오면 두세 판 만에 순서를 외워 선택이 실행 절차가 된다.

### 2.5 피자 판정과 절차적 명명

**8라운드에 진입하면 도우가 구워진다.** 올린 토핑 수와 무관하게 무조건 일어난다 — 조건 미달로 못 굽는 경우는 없다.

- **주 맛** = 가장 많이 올린 맛
- **부 맛** = 두 번째로 많은 맛
- 동수면 **능력치 친화도가 높은 쪽이 이긴다** — 도우 숙성이 여기서 한 번 더 방향을 잡는다
- 토핑이 하나도 없어도 친화도만으로 정해진다

25종을 하나씩 만들지 않는다. **주 맛 5행 + 부 맛 5행 = 10행의 데이터**와 판정 함수 1개로 25종이 나온다.

굽기를 막지 않는 대신 **완성도를 차등한다.**

| 등급 | 조건 | 보너스 |
|---|---|---|
| 완성 | 한 맛 3개, 또는 주 맛 2 + 부 맛 | 전량 + 부 풍미 특성 |
| 미완 | 주 맛 2, 부 맛 없음 | 70% + 특성 |
| 밋밋 | 주 맛 1 이하 | 40%, 특성 없음 |

**이름은 실제 올린 재료로 그때그때 만든다.**

```
[수식어] [소스] [대표 재료] 피자

완벽한 트러플 가지 피자
대담한 핫칠리 판체타 피자
뒤죽박죽 할라피뇨 피자
맨 도우
```

- 수식어는 완성도와 맛 충돌에서 나온다 (완벽한 / 대담한 / 어수선한 / 뒤죽박죽 / 밋밋한)
- 대표 재료는 **고기가 있으면 고기가 이긴다** — 피자는 보통 고기로 불린다(페퍼로니 피자, 불고기 피자)
- 소스는 보스를 잡아야 나오므로, 클리어하면 그 소스가 발려 완성된다

도감은 두 겹이다. **25칸 고정 그리드**가 모으는 목표를 보여 주고, **절차적 이름**이 같은 칸이라도 판마다 다른 결과를 만든다. 칸만 있으면 25번 만에 질리고, 이름만 있으면 무엇을 모으는지 알 수 없다.

---

## 3. 개발 과정의 AI

### 3.1 역할 분담

| 사람 (디렉터) | AI |
|---|---|
| 게임 규칙 결정 | 전체 코드 작성 |
| 재미 판정 | 규칙의 결과를 수치로 검증 |
| 우선순위 판단 | 설계 충돌·결함 지적 |
| 최종 채택 | 문서 작성 |

**코드는 전량 AI가 작성했다.** 사람은 규칙을 정하고 방향을 잡았다. 커밋 메시지에 무엇을 왜 바꿨는지가 남아 있다.

### 3.2 밸런스 시뮬레이터 — `tools/sim.ts`

수치를 감으로 정하지 않았다. **실제 전투·토핑·보상·굽기 코어를 그대로 불러 쓰는 시뮬레이터**로 1,200판을 자동으로 돌렸다.

```bash
npm run sim
```

핵심은 시뮬레이터가 게임 규칙을 **베끼지 않고 import 한다**는 점이다. 수치를 따로 적어 두면 코드를 고칠 때마다 어긋나서 측정값이 거짓말을 한다.

측정 항목:
- 재료 정책 5종 × 주사위 6면 × 40판
- 보상 정책 4종
- 정책별 클리어율, 평균 소요 턴, 완성도 분포
- 실패 원인(무너짐 / 시간 초과)

난수는 시드 기반(mulberry32)이라 **같은 시드면 같은 결과가 나온다.**

---

## 4. AI가 잡아낸 설계 결함 6건

이 문서에서 가장 중요한 부분이다. 여섯 건 모두 **사람이 규칙을 정했고, AI가 그 규칙의 결과를 측정해 문제를 드러냈고, 규칙이 바뀌었다.**

### 4.1 회복 시점이 보상 조합 간 난이도를 어긋나게 만들고 있었다

보상 지점은 판마다 `[1, 3, 9]` 또는 `[2, 4, 8]` 중 하나로 추첨된다. 처음에는 **두 조합의 난이도가 같다**고 판단했다. 근거는 두 조합 모두 5라운드 중간보스와 10라운드 보스 진입 시점의 누적 카드가 같다는 것이었다.

시뮬레이션 결과 실패가 7~9라운드에 몰렸다.

```
1-7  201판  █████████████████████
1-8  329판  ███████████████████████████████████
1-9  379판  ████████████████████████████████████████
```

원인은 카드 수가 아니라 **회복 간격**이었다. 당시 회복을 보상 지점에 묶어 뒀는데, 조합 `[1,3,9]`는 4~9 **여섯 라운드를 회복 없이** 통과해야 했다. `[2,4,8]`은 최대 네 라운드였다.

카드 수만 보고 "난이도가 같다"고 한 판단이 틀렸다.

**조치:** 회복을 보상 지점에서 떼어 **라운드 클리어마다 정률 회복**으로 바꿨다. 조합과 무관해졌다.

### 4.2 계열을 흩뿌리는 쪽이 모으는 쪽보다 유리했다

설계 의도는 "맛을 모아야 강해진다"였다. 측정값은 반대였다.

| 정책 | 클리어율 |
|---|---|
| 집중 | 50% |
| **분산** | **61%** |

원인은 **완성도 보너스가 아직 구현되지 않아서**였다. 보너스가 없으면 흩뿌릴수록 쓸 수 있는 기술 종류만 늘어난다. 모을 이유가 게임 안에 존재하지 않았다.

여기서 중요한 판단이 하나 있었다. 이 시점에 적 수치를 조정하고 싶은 유혹이 있었지만, **미완성 시스템에 맞춰 튜닝하는 것이 되므로 보류**했다. 보너스를 먼저 구현하고 다시 측정해 64% / 47%로 역전을 해소했다.

### 4.3 시작 능력이 승패를 확정하고 있었다

> 당시에는 주사위를 굴리는 화면이었다. 지금은 도우 숙성으로 바뀌었지만 6가지 결과 구조는 같다.

기획의 두 축은 이렇다.

1. 주사위는 초반에 큰 영향을 미친다
2. 주사위가 나빠도 선택을 잘하면 이어갈 수 있다

측정해 보니 2번이 성립하지 않았다.

```
변경 전:  눈1 100%  눈2 0%  눈3 100%  눈4 100%  눈5 0%  눈6 0%
```

중간이 없었다. **주사위를 굴리는 순간 승패가 정해졌다.**

**조치:** 완성도 보너스와 보상 등급을 구현하고, 마나까지 겸해 혼자 강했던 눈 3을 낮추고, 처지던 눈 6의 횟수를 올렸다. 현재는 45~73%로, 눈이 유리함을 주되 결과를 정하지 않는다.

### 4.4 "아무거나 다 올리기"가 최적이었다 — 맛 충돌 규칙의 탄생

피자 테마로 전환한 뒤 다시 재 보니, 재료를 **가리지 않고 여섯 칸을 다 채우는 쪽**(98%)이 **한 맛을 모으는 쪽**(75%)을 크게 이겼다. 토핑이 주는 능력치가 크고 무게 대가는 작아서, 고민할 이유가 없었다. 그러면 '지나치기' 버튼이 죽는다.

**조치 1 — 맛 충돌.** 매콤+새콤, 진한+향긋을 한 판에 올리면 부딪치게 했다.

처음에는 완성도 배율에만 페널티를 걸었는데 **99% → 99%로 거의 움직이지 않았다.** 생짜 능력치가 완성도 보너스보다 훨씬 컸기 때문이다. 굽기 시점에 **능력치 자체를** 충돌 1쌍당 14% 깎도록 바꾸자 비로소 반응했다.

**조치 2 — 빠진 전략을 재기.** 그런데 여전히 '한 맛 집중'이 앞서지 못했다. 시뮬레이터에 **정책 하나가 빠져 있었기 때문**이다. 충돌 쌍은 두 개뿐이고 담백은 어느 것과도 안 부딪치므로, "한 맛만 고집"과 "아무거나"  사이에 **"안 부딪치는 것만 골라 채우기"** 라는 전략이 존재했다. 추가해 재 보니 이쪽이 최적이었다.

설계 교훈이 **"한 맛만 모아라"가 아니라 "칸은 채우되 궁합을 봐라"**로 바뀌었다. 피자 게임에 훨씬 맞는 교훈이다.

**조치 3 — 난이도 재조정.** 최적 전략이 99%라 긴장이 없었다. 토핑이 능력치를 주기 시작하면서 플레이어가 세졌는데 적 규격은 그 전 기준이었다. 체력 1.55배, 공격력 1.45배로 올렸다.

**최종 측정 (1,200판)**

| 재료 정책 | 클리어율 |
|---|---|
| **안 부딪치는 것만** | **60%** ← 최적 |
| 무조건 올림 | 42% |
| 한 맛 집중 | 40% |
| 가벼운 것만 | 39% |
| 전부 지나치기 | 1% |

최적과 차선의 격차가 18%p, 아무것도 안 하면 1%다. 보상 쪽도 '무리한 반죽만 고르기'가 15%로 확실한 오답이 됐다.

### 4.5 같은 능력치가 화면마다 다른 이름으로 나오고 있었다

캐릭터 생성 화면에서는 "마법력", 보상 화면에서는 "반죽 탄력". **같은 값인데 플레이어가 알 수 없었다.** 테마를 바꾸면서 화면마다 이름표를 따로 두었기 때문이다.

**조치:** `character.ts`를 유일한 출처로 삼고 나머지 사본을 지웠다. 화면마다 따로 적어 두면 또 갈린다.

> **부수 발견:** 피자 테마로 리팩터링할 때 시뮬레이터가 조용히 깨져 있었다. `tools/` 가 `tsconfig.json` 의 검사 대상이 아니라 타입 오류로 잡히지 않았다. 밸런스를 재려다 발견했다. 측정 도구도 코드이므로 같이 검사받아야 한다는 교훈이다.

### 4.6 같은 재료가 한 판에 반복되고 있었다

야채가 30종일 때, 한 판에서 같은 재료를 두 번 만날 확률이 **96%** 였다. 일반 라운드가 여덟 번이라 14마리를 뽑는데 30종은 너무 적었다.

먼저 야채를 60종으로 늘렸다. 재 보니 여전히 **77.6%** 였다 — 늘리는 것만으로는 부족했다.

그래서 만난 재료를 판 단위로 기록해 제외했다. 여기서 두 가지를 검토했다.

**"만났다"의 기준.** 도우에 올린 것만 뺄지, 마주친 것을 다 뺄지. **마주친 것 전부**로 잡았다. 지나친 재료가 다시 나오면 같은 판단을 또 하게 되는데, 그건 새로운 선택이 아니다.

**한 맛 모으기가 어려워지지 않는가.** 이게 진짜 걱정거리였다. 완성 등급은 한 맛 3개가 필요한데, 할라피뇨를 뽑으면 그게 풀에서 빠진다. 3,000판을 재 봤다.

| | 변경 전 | 변경 후 |
|---|---|---|
| 같은 재료 재등장 | 77.6% | **0.0%** |
| 맛 분포 | 고름 | 19.7~20.2% (고르면 20%) |
| 특정 맛 등장 수 | 평균 2.8 | **평균 2.81** |

**평균은 그대로다.** 맛마다 12종이라 하나 빠져도 11종이 남기 때문이다. 바뀌는 것은 분산뿐이다 — 비복원 추출이라 운 좋게 5개 몰리는 판도, 하나도 안 나오는 판도 드물어진다. 매 판이 조금 더 비슷해지는 대신 덜 억울해진다.

풀이 바닥나면 제외를 푼다. 지금은 60종에 14마리라 일어나지 않지만, 라운드를 늘리면 뽑을 게 없어 라운드가 만들어지지 않는다.

---

## 5. 주요 지시 사항

사람이 AI에게 준 규칙 중 설계를 크게 바꾼 것들이다. 각 지시는 커밋 하나 이상으로 이어진다.

| 지시 | 결과 |
|---|---|
| "주사위는 능력치를 설정하는 것" | 축복 6종을 폐기하고 능력치 5종 + 등급 확정으로 재설계 |
| "보상은 3개 중 1개" | 주사위가 선택지 수를 바꾸던 규칙 폐기 |
| "보상은 1개 고르면 바로 다음 라운드" | 지점당 3장 → 1장. 한 장의 무게를 키워 보정 |
| "1,3,9 또는 2,4,8 중 랜덤" | 보상 지점 조합 추첨 |
| "전직은 8라운드에서 무조건" | 조건 폐기. 대신 맛 집중도로 완성도를 차등 |
| "턴당 10초, 라운드 100초, 보스 150초" | 제한 시간 두 겹. 100÷10=10턴이라 시간 제한이 턴 예산을 겸한다 |
| "피자를 만드는 게임으로 바꾸자" | 테마 전면 전환. 계열 5종 자리에 맛 5종을 넣어 판정 코드를 살렸다 |

AI가 지시를 그대로 받기만 한 것은 아니다. **지시가 다른 규칙과 충돌할 때는 충돌 지점을 먼저 제시했다.** 예를 들어 "보상을 3라운드마다"라는 지시는 그대로 따르면 총 보상이 3회가 되어, 당시 규칙(전직에 스킬 3개 필요)에서는 선택의 여지가 사라졌다. 이를 지적해 정리했고, 이후 전직 규칙이 바뀌면서 제약이 해소되자 원래 지시대로 되돌렸다.

---

## 6. 외부 에셋 · 오픈소스

### 6.1 외부 에셋 — 폰트 1종

| 에셋 | 출처 | 라이선스 |
|---|---|---|
| Galmuri11 (Regular / Bold) | https://github.com/quiple/galmuri | SIL Open Font License 1.1 |

라이선스 전문은 `src/assets/fonts/OFL.md` 에 포함했다.

**폰트를 뺀 나머지 그림은 전부 CSS로 그렸다.** 이미지·사운드 파일이 하나도 없다.

- `src/ui/CharacterSprite.tsx` / `.css` — 도우 (둥근 / 네모)
- `src/ui/Dice.tsx` / `.css` — 3×3 그리드로 그린 주사위 눈
- `src/ui/Battle.css` — 적, 게이지, 상태 표시

폰트는 `font-display: swap` 으로 불러온다. 도착을 기다리며 글자를 감추면 첫 화면이 비어 보이므로, 시스템 폰트로 먼저 그리고 도착하면 바꿔 끼운다.

### 6.2 오픈소스 의존성

| 패키지 | 라이선스 | 용도 |
|---|---|---|
| react / react-dom 18.3 | MIT | UI |
| vite 6 | MIT | 번들러 |
| typescript 5.6 | Apache-2.0 | 타입 검사 |
| @vitejs/plugin-react | MIT | 빌드 |
| esbuild | MIT | 시뮬레이터 실행 |

런타임 의존성은 React뿐이다. 빌드 결과물은 **JS 189KB + CSS 27KB = 약 216KB (gzip 69KB)** 이고, 폰트 671KB가 뒤이어 따라붙는다. `font-display: swap` 이라 폰트를 기다리지 않고 바로 플레이할 수 있다.

---

## 7. 재현 방법

```bash
npm install
npm run dev      # 개발 서버
npm run build    # 프로덕션 빌드 (타입 검사 포함)
npm run sim      # 밸런스 시뮬레이션 1,200판
```

`main` 브랜치에 푸시하면 GitHub Actions가 빌드 후 Pages로 자동 배포한다 (`.github/workflows/deploy.yml`). 빌드 스크립트에 `tsc -b`가 물려 있어 타입 오류가 있으면 배포가 중단된다.

---

## 8. 한계와 남은 과제

정직하게 적는다.

- **게임 내 AI는 규칙 기반이다.** 학습 모델이나 LLM 추론은 들어 있지 않다. §2.1의 이유로 의도적으로 배제했다.
- **밸런스 수치는 시뮬레이션 기준이다.** 시뮬레이터의 전투 판단은 사람보다 단순하므로, 실제 플레이어의 클리어율은 이보다 높을 가능성이 크다.
- **한 턴의 체감 시간은 아직 실측하지 않았다.** 제한 시간 100초가 적절한지는 사람이 플레이해 봐야 확정된다.
- **적 특기는 맛마다 하나로 고정돼 있다.** 통상 공격 + 맛별 특기 하나(§2.3)까지는 있지만, 같은 맛 안에서 패턴을 더 늘리면 전투의 깊이가 늘어난다.
